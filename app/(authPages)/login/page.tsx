import LoginContainer from '@/components/containers/LoginContainer';

export const metadata = {
    title: 'Login',
    description: 'Access the weather widget by logging in.',
};

export default function LoginPage() {
    return <LoginContainer />;
}
