import SignupContainer from '@/components/containers/SignupContainer';

export const metadata = {
    title: 'Sign Up',
    description: 'Create an account to use the weather widget.',
};

export default function LoginPage() {
    return <SignupContainer />;
}
